spring.application.name=PaymentProcessingSystem
spring.datasource.url=jdbc:mysql://localhost:3306/paymentprocessingsystem?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=n3u3da!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.port=8080
# Run schema.sql and data.sql on startup (creates tables + seeds if missing)
spring.sql.init.mode=always