# MySQL root password – used by both the db container and the backend.
# Change this before deploying to any shared/production environment.
MYSQL_ROOT_PASSWORD=n3u3da!

# Override the gateway URL only if running the simulator outside Docker.
# Default (http://simulator:8081) works with docker compose up out of the box.
# GATEWAY_BASE_URL=http://simulator:8081
